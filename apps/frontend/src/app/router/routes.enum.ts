export enum ROUTES {
  HOME = "/",
  AUTH = "/auth",
  LOGIN = "sign-in",
  REGISTER = "sign-up",
  PROJECTS = "projects",
  CLIENTS = "clients",
  CONTACTS = "contacts",
  BILLING = "billing",
  TASKS = "tasks",
  USER = "users",
  SETTINGS = "settings",
  NOTIFICATIONS = "notifications",
  PLANS = "plans",
  NO_ACCESS = "no-access",
}

export enum TASKS_VIEW {
  TABLE = "t",
  LIST = "l",
  BOARD = "b",
}

export enum CONTACTS_VIEW {
  FRIENDS = "f",
  CLIENTS = "c",
}
