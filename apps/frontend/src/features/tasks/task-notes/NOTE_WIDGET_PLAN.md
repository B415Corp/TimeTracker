# План разработки кастомного виджета заметок с вложенностью и dnd-kit

## Общая цель
Создать легковесный редактор заметок с поддержкой:
- Вложенности (отступы, parent/child)
- Drag-and-drop для перемещения и изменения вложенности (dnd-kit)
- Изменения типа строки (заголовок, список, ссылка, файл, текст)
- Интуитивного создания/удаления строк (Enter, Backspace)
- Сериализации структуры для хранения в БД и восстановления

---

## Этап 1. Базовая структура и отображение
- [x] Описать структуру данных (NoteLine: id, parentId, order, type, content)
- [x] Реализовать отображение списка строк с вложенностью (отступы по уровню)
- [x] Реализовать компонент строки (input/textarea, отображение типа)
- [x] Добавить базовые действия: редактирование текста, добавление/удаление строки (Enter/Backspace)

## Этап 2. Drag-and-drop (dnd-kit)
- [x] Подключить dnd-kit к списку строк
- [x] Реализовать перемещение строк drag-and-drop
- [x] Реализовать изменение вложенности через dnd (перетаскивание вправо/влево меняет parentId) *(визуально, логика вложенности будет доработана на следующих этапах)*
- [x] Визуализировать область drop и уровень вложенности при dnd

## Этап 3. Горячие клавиши для вложенности
- [x] Реализовать Tab (увеличить вложенность — сделать дочерней предыдущей)
- [x] Реализовать Shift+Tab (уменьшить вложенность — поднять на уровень выше)
- [x] Обновить структуру данных при изменении вложенности

## Этап 4. Типы строк и тулбар
- [x] Добавить выбор типа строки (заголовок, список, ссылка, файл, текст)
- [x] Реализовать тулбар или контекстное меню для смены типа
- [x] Для ссылок — поддержка url, для файлов — базовая заглушка (позже интеграция с загрузкой)

## Этап 5. Сериализация и интеграция с БД
- [ ] Реализовать функции сериализации структуры (flat/tree → JSON)
- [ ] Реализовать функции восстановления структуры из БД
- [ ] Интегрировать с текущим API заметок (создание/обновление/загрузка)

## Этап 6. Улучшения и UX
- [ ] Визуальные улучшения (иконки, плавные анимации dnd, подсветка активной строки)
- [ ] Drag-to-delete (перетаскивание в область удаления)
- [ ] Поддержка вложенных списков и других сложных структур (по необходимости)
- [ ] Тестирование на edge-cases (циклы, некорректная вложенность)

## Этап 7. Улучшения вложенности и dnd (по результатам тестирования)
- [x] Перенести кнопки смены типа строки в выпадающее меню внутри строки
- [x] Реализовать вложенность через dnd (drag вправо/влево меняет parentId, визуализация уровня вложенности при dnd)
- [x] При перетаскивании строки с дочерними, переносить всё поддерево вместе с родителем
- [x] Корректно обновлять parentId/order для всех перемещённых строк
- [ ] (Опционально) Ограничить максимальный уровень вложенности
- [ ] (Опционально) Запретить вложенность определённых типов (например, файл в файл)

---

## Пример структуры NoteLine
```ts
interface NoteLine {
  id: string;
  parentId: string | null;
  order: number;
  type: "heading" | "list" | "text" | "link" | "file";
  content: string;
  // ...доп. поля
}
```

---

## Примечания
- dnd-kit уже установлен, используем его для drag-and-drop.
- На каждом этапе можно делать отдельные коммиты и PR.
- После каждого этапа — ручное тестирование и рефакторинг. 